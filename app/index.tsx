import { SafeAreaView, StyleSheet, Text, View } from "react-native";
import React from "react";
import { Redirect } from "expo-router";
import SplashOne from "./SplashScreen";

const Page = () => {
 return <SplashOne />;
};

export default Page;

const styles = StyleSheet.create({});
